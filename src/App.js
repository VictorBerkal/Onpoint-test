import React from 'react';
import Layout from './Layout/Layout';
import Slider from './components/Slider';

const App = props => {
  return(
    <Layout>
        <Slider/>
    </Layout>
  );
}

export default App;
